# Fichier de test
# Les lignes de commentaires commencent par #
procedure principale()
  i = 1;
  ecrire("Entrer j : ");
  lire(j);
  ecrire("i = ", i, " j = ",j);
  i=i+1;
  ecrire(i);
finproc
# Commentaire Final
